#include <stdio.h>
/**
 * main- main entry point
 * program printts a line of string using printif instruction
 * Return: 0(Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
